server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gestao_comercial?useSSL=false&serverTimezone=UTC
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  security:
    user:
      name: admin
      password: admin123
